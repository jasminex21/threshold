# requirements for deepseize's development environment
name: threshold
dependencies:
  - numpy = 1.23.4
  - scipy
  - matplotlib
  - ipython
  - notebook
  # testing
  - pytest
  - pytest-cov
    # Linting & format
  - isort
  - mypy
  - pylint
  # pypi packages
  - pip
  - pip:
    - pytest-lazy-fixture
    - openseize
